services:
  - type: web
    name: skiza-spin-win
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn skiza_project.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
